@model AliMartinCv.DataLayer.Entities.BlogGroup
@{
    ViewData["Title"] = "Delete Existed Group";
    Layout = "/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<form method="post">
    <div class="card shadow mb-4">
        <input asp-for="BlogGroupId" value="@ViewBag.Id" hidden="hidden" />
        <input asp-for="BlogGroupParentId" value="@ViewBag.subId" hidden="hidden" />
        <input asp-for="BlogGroupTitle" value="@ViewBag.GroupTitle" hidden="hidden" />
        <div class="p-5">
            <div class="text-center">
                <h1 class="h4 text-gray-900 mb-4">Delete Blog Groupe</h1>
            </div>

            <div class="form-group">
                <input asp-for="BlogGroupTitle" type="text" class="form-control form-control-user disabled" disabled="disabled" id="exampleInputEmail" aria-describedby="emailHelp" placeholder="Enter Group Name...">
            </div>
            <button type="submit" class="btn btn-Danger btn-user btn-block">Delete !</button>
            <hr>

        </div>
    </div>
</form>